This example is just a way to update several SMC instances. This is not the only way to do it.

Modules are used here to manage each SMC and common data are in root part.

There is other way to use modules to achieve this in more efficient sashion depending of the goal of the deployment.
(just some setup shared or lot of common ressources)

Warning: This example do not rely on a proper knowledge of terraform solution.
